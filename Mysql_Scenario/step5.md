Creating few more files which is required for MySql. We already have refernce files, we can just copy it to the appropriate location.

Task 1:
Copy the logo file.<br>
`cp ~/test/Logo_mysql.png ~/Mysql`{{execute}}
